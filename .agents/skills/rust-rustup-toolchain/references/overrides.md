# Overrides

## Set Override

```bash
rustup override set nightly
rustup override set stable
rustup override set 1.70.0
```

## Unset Override

```bash
rustup override unset
```

## Show Current

```bash
rustup show
```

## Per-Directory

Overrides apply to the current directory and subdirectories.
